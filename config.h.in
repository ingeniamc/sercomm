/*
 * Copyright (C) the libsercomm contributors. All rights reserved.
 *
 * This file is part of libsercomm, distributed under the GNU GPL v2 with
 * a Linking Exception. For full terms see the included COPYING file.
 */

#ifndef PUBLIC_SERCOMM_CONFIG_H_
#define PUBLIC_SERCOMM_CONFIG_H_

/** libsercomm version. */
#define SER_VERSION "${PROJECT_VERSION}${PROJECT_VERSION_EXTRA}"

/** libsercomm is built as a static library. */
#cmakedefine SER_STATIC

#endif
